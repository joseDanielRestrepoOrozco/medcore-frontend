## Frontend (Vite) environment variables
# Base URL del API Gateway o Backend. Ejemplos:
# - Desarrollo con gateway local: http://localhost:3000/api/v1
# - Relativo con proxy de Vite (vite.config.ts): /api/v1
#
# Si no se define, el frontend usará por defecto "/api/v1".
VITE_API_BASE=/api/v1

